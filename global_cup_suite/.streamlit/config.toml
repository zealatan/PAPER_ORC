# 다크 올리브 테마를 네이티브 기본값으로 고정한다. 이렇게 하면 inject_css가
# 직접 칠하지 않는 네이티브 위젯(라디오/셀렉트/nav 등)도 밝은 기본 테마로 새지 않아
# "밝은 배경 + 흰 글씨" 가독성 문제가 사라진다.
[theme]
base = "dark"
primaryColor = "#8dbb55"
backgroundColor = "#0b1008"
secondaryBackgroundColor = "#141d10"
textColor = "#fff5dc"
