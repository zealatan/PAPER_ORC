{
  "name": "@motion-studio/scenarios",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "High-end cinematic scenario Shorts (720x1280/15fps) built on the existing engine.",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "scenarios:generate": "tsx src/cli.ts generate",
    "scenarios:preview": "tsx src/cli.ts preview",
    "scenarios:render": "tsx src/cli.ts render",
    "scenarios:validate": "tsx src/cli.ts validate",
    "scenarios:gallery": "tsx src/cli.ts gallery",
    "scenarios:all": "tsx src/cli.ts all"
  },
  "dependencies": {
    "@motion-studio/components": "workspace:*",
    "@motion-studio/core": "workspace:*",
    "@motion-studio/renderer": "workspace:*",
    "@motion-studio/renderer-core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.1"
  }
}
